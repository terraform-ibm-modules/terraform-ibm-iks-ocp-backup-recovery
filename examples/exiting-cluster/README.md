# Existing Openshift/Kubernetes example

This example provisions a fully integrated Backup & Recovery environment with policies, source registration, connectivity, and data source connector deployment on a existing cluster

A existing openshift/kubernetes example that will provision the following:

- A new resource group, if an existing one is not passed in.
- A new Backup & Recovery instance.
- A data source connection to integrate the cluster with the Backup & Recovery service.
